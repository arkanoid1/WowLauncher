
#include "Dialog.hpp"

namespace gui {
    Dialog::Dialog(Widget *parent) {
        // DWORD dwStyle = WS_
    }

    DialogResult::Enum Dialog::show() {

        return DialogResult::OK;
    }
}
